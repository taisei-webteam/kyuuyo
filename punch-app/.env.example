DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Optional: use a different API origin during local frontend-only development.
# In Vercel, leave this empty so the app calls same-origin /api routes.
VITE_API_BASE_URL=

# Set true only when using built-in demo data.
VITE_DEMO_MODE=false
